{
  "name": "@ponko2/botkit-heroku-keepalive",
  "description": "Botkit Heroku Keepalive",
  "version": "0.4.0",
  "author": "Takahito Nakano",
  "dependencies": {
    "request": "^2.69.0"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "botkit": "^0.0.13",
    "eslint": "^2.4.0",
    "eslint-config-ponko2": "^2.2.0"
  },
  "files": [
    "lib"
  ],
  "keywords": [
    "botkit"
  ],
  "license": "MIT",
  "main": "lib/index.js",
  "peerDependencies": {
    "botkit": "^0.0.12"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ponko2/botkit-heroku-keepalive.git"
  },
  "scripts": {
    "build": "babel src --out-dir lib --source-maps inline",
    "lint": "eslint .",
    "prepublish": "npm run --if-present build",
    "watch": "babel src --out-dir lib --watch --source-maps inline"
  }
}
